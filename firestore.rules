rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allows read and write for all users for easy multi-device sync
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
